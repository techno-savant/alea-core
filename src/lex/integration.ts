// Task LEX.1–LEX.5 — Optional Lex integration
// Detected at Hooks.once('ready'). If lexicon-core is active, registers Alea-specific
// rule element types (pool-modifier, casus-modifier, grant-fortune) and resolution targets.
export {};
